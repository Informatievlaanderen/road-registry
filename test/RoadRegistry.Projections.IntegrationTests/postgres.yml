version: '3.8'

services:
  postgres:
    image: postgis/postgis:14-3.5-alpine
    ports:
      - "15433:5432"
    volumes:
      - type: tmpfs
        target: /var/lib/postgresql/data
      - type: tmpfs
        target: /dev/shm
    environment:
      POSTGRES_PASSWORD: postgres
