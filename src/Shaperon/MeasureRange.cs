namespace Shaperon
{
    public class MeasureRange
    {
        public MeasureRange(double mMin, double mMax)
        {
            MMin = mMin;
            MMax = mMax;
        }
        public double MMin { get; }
        public double MMax { get; }
    }
}
