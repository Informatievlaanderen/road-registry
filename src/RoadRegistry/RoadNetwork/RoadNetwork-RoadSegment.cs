namespace RoadRegistry.RoadNetwork;

public partial class RoadNetwork
{
    private void ModifyRoadSegment(object command)
    {
        // ************ vervanging van de huidige Core classes per command

        // verify before (guard)

        // generate + apply events

        // verify after

        // return events
    }
}
