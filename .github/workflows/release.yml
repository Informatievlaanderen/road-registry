name: 2. Release
run-name: Release

on:
  workflow_dispatch:

concurrency: Release    

env:
  SA_PASSWORD: ${{ secrets.SA_PASSWORD }}
  NPM_TOKEN: ${{ secrets.VBR_WEBCOMP_NPM_TOKEN }}

jobs:

  set-release-version:
    if: github.repository_owner == 'Informatievlaanderen'
    name: Decide next version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.set-version.outputs.version }}

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3
      with:
          persist-credentials: false

    - name: Setup Node.js
      uses: actions/setup-node@v4

    - name: Node version
      shell: bash
      run: node --version

    - name: Install NPM dependencies
      shell: bash
      run: npm ci
      env:
        NPM_TOKEN: ${{ secrets.VBR_WEBCOMP_NPM_TOKEN }}

    - name: Run Semantic Release Dry-Run
      shell: bash
      run: npx semantic-release --dry-run
      env:
        BUILD_DOCKER_REGISTRY: ${{ secrets.VBR_BUILD_DOCKER_REGISTRY_TST }}
        GITHUB_TOKEN: ${{ secrets.VBR_GIT_RELEASE_TOKEN }}
        GIT_COMMIT: ${{ github.sha }}
        GIT_USERNAME: ${{ secrets.VBR_GIT_USER }}
        GIT_AUTHOR_NAME: ${{ secrets.VBR_GIT_USER }}
        GIT_COMMITTER_NAME: ${{ secrets.VBR_GIT_USER }}
        GIT_EMAIL: ${{ secrets.VBR_GIT_EMAIL }}
        GIT_AUTHOR_EMAIL: ${{ secrets.VBR_GIT_EMAIL }}
        GIT_COMMITTER_EMAIL: ${{ secrets.VBR_GIT_EMAIL }}
    
    - name: Set Release Version
      id: set-version
      run: |
        [ ! -f semver ] && echo none > semver
        echo $(cat semver)
        echo ::set-output name=version::$(cat semver)
        echo RELEASE_VERSION=$(cat semver) >> $GITHUB_ENV
        echo SA_PASSWORD=${{ secrets.SA_PASSWORD }} >> $GITHUB_ENV
      shell: bash

  build-backoffice-api:
    name: Build BackOffice Api    
    uses: ./.github/workflows/build-image-sql.yml
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      registry: road-registry
      build-target: Containerize_BackOfficeApi
      image-file: rr-backoffice-api-image.tar
      image-name: backoffice-api
      test-project: RoadRegistry.BackOffice.Api.Tests
      build-project: RoadRegistry.BackOffice.Api
      semver: ${{ needs.set-release-version.outputs.version }}      
    secrets: inherit

  build-backoffice-ui:
    name: Build BackOffice UI
    uses: ./.github/workflows/build-image-nodejs.yml
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      registry: road-registry
      build-target: Publish_BackOfficeUI
      containerize-target: Containerize_BackOfficeUI
      image-file: rr-backoffice-ui-image.tar
      image-name: backoffice-ui
      build-project: RoadRegistry.BackOffice.UI
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  build-backoffice-eventhost:
    name: Build BackOffice Event Host
    uses: ./.github/workflows/build-image-sql.yml
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      registry: road-registry
      build-target: Containerize_BackOfficeEventHost
      image-file: rr-backoffice-eventhost-image.tar
      image-name: backoffice-eventhost
      test-project: RoadRegistry.BackOffice.EventHost.Tests
      build-project: RoadRegistry.BackOffice.EventHost
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  build-backoffice-extracthost:
    name: Build BackOffice Extract Host
    uses: Informatievlaanderen/build-pipeline/.github/workflows/build-image.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      registry: road-registry
      build-target: Containerize_BackOfficeExtractHost
      image-file: rr-backoffice-extracthost-image.tar
      image-name: backoffice-extracthost
      test-project: RoadRegistry.BackOffice.ExtractHost.Tests
      build-project: RoadRegistry.BackOffice.ExtractHost
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  build-backoffice-commandhost:
      name: Build BackOffice Command Host
      uses: ./.github/workflows/build-image-sql.yml
      needs: [ set-release-version ]
      if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
      with:
        registry: road-registry
        build-target: Containerize_BackOfficeCommandHost
        image-file: rr-backoffice-commandhost-image.tar
        image-name: backoffice-commandhost
        test-project: RoadRegistry.BackOffice.CommandHost.Tests
        build-project: RoadRegistry.BackOffice.CommandHost
        semver: ${{ needs.set-release-version.outputs.version }}
      secrets: inherit
  
  build-adminhost:
      name: Build Admin Host
      uses: Informatievlaanderen/build-pipeline/.github/workflows/build-image.yml@main
      needs: [ set-release-version ]
      if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
      with:
        registry: road-registry
        build-target: Containerize_AdminHost
        image-file: rr-adminhost-image.tar
        image-name: adminhost
        test-project: RoadRegistry.AdminHost.Tests
        build-project: RoadRegistry.AdminHost
        semver: ${{ needs.set-release-version.outputs.version }}
      secrets: inherit

  build-projector:
    name: Build Projector
    uses: Informatievlaanderen/build-pipeline/.github/workflows/build-image.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      registry: road-registry
      build-target: Containerize_Projector
      image-file: rr-projector-image.tar
      image-name: projector
      test-project: RoadRegistry.Projector.Tests
      build-project: RoadRegistry.Projector
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  build-projectionhost-editor:
    name: Build Editor Projection Host
    uses: Informatievlaanderen/build-pipeline/.github/workflows/build-image.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      registry: road-registry
      build-target: Containerize_EditorProjectionHost
      image-file: rr-editor-projectionhost-image.tar
      image-name: editor-projectionhost
      test-project: RoadRegistry.Editor.ProjectionHost.Tests
      build-project: RoadRegistry.Editor.ProjectionHost
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  build-projectionhost-product:
    name: Build Product Projection Host
    uses: Informatievlaanderen/build-pipeline/.github/workflows/build-image.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      registry: road-registry
      build-target: Containerize_ProductProjectionHost
      image-file: rr-product-projectionhost-image.tar
      image-name: product-projectionhost
      test-project: RoadRegistry.Product.ProjectionHost.Tests
      build-project: RoadRegistry.Product.ProjectionHost
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  build-projectionhost-producersnapshot:
    name: Build Producer Snapshot Projection Host
    uses: Informatievlaanderen/build-pipeline/.github/workflows/build-image.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      registry: road-registry
      build-target: Containerize_ProducerSnapshotProjectionHost
      image-file: rr-producer-snapshot-projectionhost-image.tar
      image-name: producer-snapshot-projectionhost
      test-project: RoadRegistry.Producer.Snapshot.ProjectionHost.Tests
      build-project: RoadRegistry.Producer.Snapshot.ProjectionHost
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  build-synchost:
    name: Build Sync Host
    uses: Informatievlaanderen/build-pipeline/.github/workflows/build-image.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      registry: road-registry
      build-target: Containerize_SyncHost
      image-file: rr-synchost-image.tar
      image-name: synchost
      test-project: RoadRegistry.SyncHost.Tests
      build-project: RoadRegistry.SyncHost
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  build-projectionhost-syndication:
      name: Build Syndication Projection Host
      uses: Informatievlaanderen/build-pipeline/.github/workflows/build-image.yml@main
      needs: [ set-release-version ]
      if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
      with:
        registry: road-registry
        build-target: Containerize_SyndicationProjectionHost
        image-file: rr-syndication-projectionhost-image.tar
        image-name: syndication-projectionhost
        test-project: RoadRegistry.Syndication.ProjectionHost.Tests
        build-project: RoadRegistry.Syndication.ProjectionHost
        semver: ${{ needs.set-release-version.outputs.version }}
      secrets: inherit
  
  build-projectionhost-wms:
    name: Build WMS Projection Host
    uses: Informatievlaanderen/build-pipeline/.github/workflows/build-image.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      registry: road-registry
      build-target: Containerize_WmsProjectionHost
      image-file: rr-wms-projectionhost-image.tar
      image-name: wms-projectionhost
      test-project: RoadRegistry.Wms.ProjectionHost.Tests
      build-project: RoadRegistry.Wms.ProjectionHost
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  build-projectionhost-wfs:
    name: Build WFS Projection Host
    uses: Informatievlaanderen/build-pipeline/.github/workflows/build-image.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      registry: road-registry
      build-target: Containerize_WfsProjectionHost
      image-file: rr-wfs-projectionhost-image.tar
      image-name: wfs-projectionhost
      test-project: RoadRegistry.Wfs.ProjectionHost.Tests
      build-project: RoadRegistry.Wfs.ProjectionHost
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  build-legacy-import:
    name: Build Legacy Import
    uses: Informatievlaanderen/build-pipeline/.github/workflows/build-image.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      registry: road-registry
      build-target: Containerize_ImportLegacy
      image-file: rr-import-legacy-image.tar
      image-name: import-legacy
      test-project: RoadRegistry.Legacy.Import.Tests
      build-project: RoadRegistry.Legacy.Import
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  build-legacy-extract:
    name: Build Legacy Extract
    uses: Informatievlaanderen/build-pipeline/.github/workflows/build-image.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      registry: road-registry
      build-target: Containerize_ExtractLegacy
      image-file: rr-extract-legacy-image.tar
      image-name: extract-legacy
      test-project: RoadRegistry.Legacy.Extract.Tests
      build-project: RoadRegistry.Legacy.Extract
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  pack-backoffice:
    name: Pack BackOffice
    uses: Informatievlaanderen/build-pipeline/.github/workflows/pack.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      pack-file: Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice
      test-project: RoadRegistry.Empty.Tests
      build-project: RoadRegistry.BackOffice
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  pack-backoffice-abstractions:
    name: Pack BackOffice Abstractions
    uses: Informatievlaanderen/build-pipeline/.github/workflows/pack.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      pack-file: Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice.Abstractions
      test-project: RoadRegistry.Empty.Tests
      build-project: RoadRegistry.BackOffice.Abstractions
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  pack-backoffice-api:
    name: Pack BackOffice Api
    uses: Informatievlaanderen/build-pipeline/.github/workflows/pack.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      pack-file: Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice.Api
      test-project: RoadRegistry.Empty.Tests
      build-project: RoadRegistry.BackOffice.Api
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  pack-backoffice-ziparchivewriters:
    name: Pack BackOffice ZipArchive Writers
    uses: Informatievlaanderen/build-pipeline/.github/workflows/pack.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      pack-file: Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice.ZipArchiveWriters
      test-project: RoadRegistry.Empty.Tests
      build-project: RoadRegistry.BackOffice.ZipArchiveWriters
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  pack-projector:
    name: Pack Projector
    uses: Informatievlaanderen/build-pipeline/.github/workflows/pack.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      pack-file: Be.Vlaanderen.Basisregisters.RoadRegistry.Projector
      test-project: RoadRegistry.Empty.Tests
      build-project: RoadRegistry.Projector
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit
  
  build-lambda-backoffice:
    name: Build Lambda BackOffice
    uses: Informatievlaanderen/build-pipeline/.github/workflows/build-lambda.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      lambda-name: lambda-backoffice
      test-project: RoadRegistry.BackOffice.Handlers.Sqs.Lambda.Tests
      build-project: RoadRegistry.BackOffice.Handlers.Sqs.Lambda
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  build-lambda-snapshot:
    name: Build Lambda Snapshot
    uses: Informatievlaanderen/build-pipeline/.github/workflows/build-lambda.yml@main
    needs: [ set-release-version ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    with:
      lambda-name: lambda-snapshot
      test-project: RoadRegistry.Snapshot.Handlers.Sqs.Lambda.Tests
      build-project: RoadRegistry.Snapshot.Handlers.Sqs.Lambda
      semver: ${{ needs.set-release-version.outputs.version }}
    secrets: inherit

  release:
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version != 'none') }}
    name: Semantic Release
    runs-on: ubuntu-latest
    needs: [ 
      set-release-version,
      pack-backoffice,
      pack-backoffice-abstractions,
      pack-backoffice-api,
      pack-backoffice-ziparchivewriters,
      pack-projector,
      build-backoffice-api,
      build-backoffice-ui,
      build-backoffice-eventhost,
      build-backoffice-extracthost,
      build-backoffice-commandhost,
      build-adminhost,
      build-projector,
      build-projectionhost-editor,
      build-projectionhost-product,
      build-projectionhost-producersnapshot,
      build-synchost,
      build-projectionhost-syndication,
      build-projectionhost-wms,
      build-projectionhost-wfs,
      build-legacy-import,
      build-legacy-extract,
      build-lambda-backoffice,
      build-lambda-snapshot 
    ]
    outputs:
      version: ${{ steps.set-version.outputs.version }}

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3
      with:
          persist-credentials: false

    - name: Cache NPM
      uses: actions/cache@v3
      env:
        cache-name: cache-npm
      with:
        path: ~/.npm
        key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-build-${{ env.cache-name }}-

    - name: Parse repository name
      run: echo REPOSITORY_NAME=$(echo "$GITHUB_REPOSITORY" | awk -F / '{print $2}' | sed -e "s/:refs//") >> $GITHUB_ENV
      shell: bash

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: |
          6.0.101
          ${{ secrets.VBR_DOTNET_VERSION }}

    - name: Setup Node.js
      uses: actions/setup-node@v4

    - name: Setup Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.x'

    - name: Node version
      shell: bash
      run: node --version

    - name: .NET version
      shell: bash
      run: dotnet --info

    - name: Python version
      shell: bash
      run: python --version

    - name: Install NPM dependencies
      shell: bash
      run: npm install --legacy-peer-deps

    - name: Install Python dependencies
      shell: bash
      run: |
        python -m pip install --upgrade pip
        pip install requests markdown argparse

    - name: Download NuGet BackOffice
      uses: actions/download-artifact@v3
      continue-on-error: false
      with:
        name: nuget-Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice-${{ needs.set-release-version.outputs.version }}
        path: dist/nuget/

    - name: Download NuGet BackOffice Abstractions
      uses: actions/download-artifact@v3
      continue-on-error: false
      with:
        name: nuget-Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice.Abstractions-${{ needs.set-release-version.outputs.version }}
        path: dist/nuget/

    - name: Download NuGet BackOffice Api
      uses: actions/download-artifact@v3
      continue-on-error: false
      with:
        name: nuget-Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice.Api-${{ needs.set-release-version.outputs.version }}
        path: dist/nuget/

    - name: Download NuGet BackOffice ZipArchiveWriters
      uses: actions/download-artifact@v3
      continue-on-error: false
      with:
        name: nuget-Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice.ZipArchiveWriters-${{ needs.set-release-version.outputs.version }}
        path: dist/nuget/

    - name: Download NuGet Projector
      uses: actions/download-artifact@v3
      continue-on-error: false
      with:
        name: nuget-Be.Vlaanderen.Basisregisters.RoadRegistry.Projector-${{ needs.set-release-version.outputs.version }}
        path: dist/nuget/

    - name: Run Semantic Release
      shell: bash
      run: npx semantic-release
      env:
        BUILD_DOCKER_REGISTRY: ${{ secrets.VBR_BUILD_DOCKER_REGISTRY_TST }}
        GITHUB_TOKEN: ${{ secrets.VBR_GIT_RELEASE_TOKEN }}
        GIT_COMMIT: ${{ github.sha }}
        GIT_USERNAME: ${{ secrets.VBR_GIT_USER }}
        GIT_AUTHOR_NAME: ${{ secrets.VBR_GIT_USER }}
        GIT_COMMITTER_NAME: ${{ secrets.VBR_GIT_USER }}
        GIT_EMAIL: ${{ secrets.VBR_GIT_EMAIL }}
        GIT_AUTHOR_EMAIL: ${{ secrets.VBR_GIT_EMAIL }}
        GIT_COMMITTER_EMAIL: ${{ secrets.VBR_GIT_EMAIL }}

    - name: Set Release Version
      id: set-version
      run: |
        [ ! -f semver ] && echo none > semver
        echo $(cat semver)
        echo ::set-output name=version::$(cat semver)
        echo RELEASE_VERSION=$(cat semver) >> $GITHUB_ENV
      shell: bash

  upload-lambda:
    name: Upload Lambda
    needs: [ release ]
    runs-on: ubuntu-latest
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.release.outputs.version != 'none') }}
    steps:
    - name: Download Lambda Backoffice
      uses: actions/download-artifact@v3
      continue-on-error: false
      with:
        name: lambda-backoffice-${{ needs.release.outputs.version }}
        path: ~/backoffice/

    - name: Download Lambda Snapshot
      uses: actions/download-artifact@v3
      continue-on-error: false
      with:
        name: lambda-snapshot-${{ needs.release.outputs.version }}
        path: ~/snapshot/

    - name: Configure AWS credentials (Test)
      uses: aws-actions/configure-aws-credentials@v1-node16
      with:
        aws-access-key-id: ${{ secrets.VBR_AWS_ACCESS_KEY_ID_TST }}
        aws-secret-access-key: ${{ secrets.VBR_AWS_SECRET_ACCESS_KEY_TST }}
        aws-region: ${{ secrets.VBR_AWS_REGION_PRD }}

    - name: Login to Amazon ECR (Test)
      uses: aws-actions/amazon-ecr-login@v1.5.3

    - name: Push Lambda functions to S3 Test
      shell: bash
      run: |
        aws s3 cp ~/backoffice/lambda.zip s3://196194905350-vbr-test-lam-rr-sqsbackoffice/$SEMVER/lambda.zip
        aws s3 cp ~/snapshot/lambda.zip s3://196194905350-vbr-test-lam-rr-sqssnapshot/$SEMVER/lambda.zip
      env:
        SEMVER: ${{ needs.release.outputs.version }}

    - name: Configure AWS credentials (Staging)
      uses: aws-actions/configure-aws-credentials@v1-node16
      with:
        aws-access-key-id: ${{ secrets.VBR_AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.VBR_AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.VBR_AWS_REGION }}

    - name: Login to Amazon ECR (Staging)
      uses: aws-actions/amazon-ecr-login@v1.5.3

    - name: Push Lambda functions to S3 Staging
      shell: bash
      run: |
        aws s3 cp ~/backoffice/lambda.zip s3://s3-vbr-$ENVIRONMENT-basisregisters-lam-wr-sqsbackofficehandlerfunction/$SEMVER/lambda.zip
        aws s3 cp ~/snapshot/lambda.zip s3://s3-vbr-$ENVIRONMENT-basisregisters-lam-wr-sqssnapshothandlerfunction/$SEMVER/lambda.zip
      env:
        SEMVER: ${{ needs.release.outputs.version }}
        ENVIRONMENT: stg

    - name: Configure AWS credentials (New Production)
      uses: aws-actions/configure-aws-credentials@v1-node16
      with:
        aws-access-key-id: ${{ secrets.VBR_AWS_ACCESS_KEY_ID_NEWPRD }}
        aws-secret-access-key: ${{ secrets.VBR_AWS_SECRET_ACCESS_KEY_NEWPRD }}
        aws-region: ${{ secrets.VBR_AWS_REGION_PRD }}

    - name: Login to Amazon ECR (New Production)
      uses: aws-actions/amazon-ecr-login@v1.5.3

    - name: Push Lambda functions to S3 New Production
      shell: bash
      run: |
        aws s3 cp ~/backoffice/lambda.zip s3://077849980894-vbr-prd-lam-rr-sqsbackoffice/$SEMVER/lambda.zip
        aws s3 cp ~/snapshot/lambda.zip s3://077849980894-vbr-prd-lam-rr-sqssnapshot/$SEMVER/lambda.zip
      env:
        SEMVER: ${{ needs.release.outputs.version }}

    - name: Configure AWS credentials (Production)
      uses: aws-actions/configure-aws-credentials@v1-node16
      with:
        aws-access-key-id: ${{ secrets.VBR_AWS_ACCESS_KEY_ID_PRD }}
        aws-secret-access-key: ${{ secrets.VBR_AWS_SECRET_ACCESS_KEY_PRD }}
        aws-region: ${{ secrets.VBR_AWS_REGION_PRD }}

    - name: Login to Amazon ECR (Production)
      uses: aws-actions/amazon-ecr-login@v1.5.3

    - name: Push Lambda functions to S3 Production
      shell: bash
      run: |
        aws s3 cp ~/backoffice/lambda.zip s3://s3-vbr-prd-basisregisters-lam-wr-sqsbackofficehandlerfunction/$SEMVER/lambda.zip
        aws s3 cp ~/snapshot/lambda.zip s3://s3-vbr-prd-basisregisters-lam-wr-sqssnapshothandlerfunction/$SEMVER/lambda.zip
      env:
        SEMVER: ${{ needs.release.outputs.version }}

  publish_to_nuget:
    needs: [ release ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.release.outputs.version != 'none') }}
    name: Publish to NuGet
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Setup .NET Core
      uses: actions/setup-dotnet@v2
      with:
        dotnet-version: ${{ secrets.VBR_DOTNET_VERSION }}

    - name: .NET version
      shell: bash
      run: dotnet --info

    - name: Download NuGet package 'backoffice'
      uses: actions/download-artifact@v3
      with:
        name: nuget-Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice-${{ needs.release.outputs.version }}
        path: ~/

    - name: Download NuGet package 'backoffice-abstractions'
      uses: actions/download-artifact@v3
      with:
        name: nuget-Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice.Abstractions-${{ needs.release.outputs.version }}
        path: ~/

    - name: Download NuGet package 'backoffice-api'
      uses: actions/download-artifact@v3
      with:
        name: nuget-Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice.Api-${{ needs.release.outputs.version }}
        path: ~/

    - name: Download NuGet package 'backoffice-ziparchivewriters'
      uses: actions/download-artifact@v3
      with:
        name: nuget-Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice.ZipArchiveWriters-${{ needs.release.outputs.version }}
        path: ~/

    - name: Download NuGet package 'projector'
      uses: actions/download-artifact@v3
      with:
        name: nuget-Be.Vlaanderen.Basisregisters.RoadRegistry.Projector-${{ needs.release.outputs.version }}
        path: ~/

    - name: Publish packages to NuGet
      shell: bash
      run: |
        dotnet nuget push ~/Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice.$SEMVER.nupkg --source nuget.org --api-key $NUGET_API_KEY
        dotnet nuget push ~/Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice.Abstractions.$SEMVER.nupkg --source nuget.org --api-key $NUGET_API_KEY
        dotnet nuget push ~/Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice.Api.$SEMVER.nupkg --source nuget.org --api-key $NUGET_API_KEY
        dotnet nuget push ~/Be.Vlaanderen.Basisregisters.RoadRegistry.BackOffice.ZipArchiveWriters.$SEMVER.nupkg --source nuget.org --api-key $NUGET_API_KEY
        dotnet nuget push ~/Be.Vlaanderen.Basisregisters.RoadRegistry.Projector.$SEMVER.nupkg --source nuget.org --api-key $NUGET_API_KEY
      env:
        SEMVER: ${{  needs.release.outputs.version }}
        WORKSPACE: ${{ github.workspace }}
        NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}

  publish_to_atlassian:
    needs: [ release ]
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.release.outputs.version != 'none') }}
    name: Publish to Atlassian
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Parse repository name
        run: echo REPOSITORY_NAME=$(echo "$GITHUB_REPOSITORY" | awk -F / '{print $2}' | sed -e "s/:refs//") >> $GITHUB_ENV
        shell: bash

      - name: Cache Paket
        uses: actions/cache@v3
        env:
          cache-name: cache-paket
        with:
          path: packages
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('paket.lock') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-

      - name: Cache Python
        uses: actions/cache@v3
        env:
          cache-name: cache-pip
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-build-${{ env.cache-name }}

      - name: Setup Python
        uses: actions/setup-python@v3
        with:
          python-version: '3.x'

      - name: Install Python dependencies
        shell: bash
        run: |
          python -m pip install --upgrade pip
          pip install requests markdown argparse

      - name: Publish to Confluence
        shell: bash
        run: ./packages/Be.Vlaanderen.Basisregisters.Build.Pipeline/Content/ci-confluence.sh
        env:
          CONFLUENCE_TITLE: ${{ env.REPOSITORY_NAME }}
          CONFLUENCE_USERNAME: ${{ secrets.VBR_CONFLUENCE_USER }}
          CONFLUENCE_PASSWORD: ${{ secrets.VBR_CONFLUENCE_PASSWORD }}

     # - name: Create Jira Release
     #   shell: bash
     #   run: ./packages/Be.Vlaanderen.Basisregisters.Build.Pipeline/Content/ci-jira.sh
     #   env:
     #     CONFLUENCE_TITLE: ${{ env.REPOSITORY_NAME }}
     #     CONFLUENCE_USERNAME: ${{ secrets.VBR_CONFLUENCE_USER }}
     #     CONFLUENCE_PASSWORD: ${{ secrets.VBR_CONFLUENCE_PASSWORD }}
     #     JIRA_PREFIX: Road
     #     JIRA_PROJECT: WR
     #     JIRA_VERSION: ${{ needs.release.outputs.version }}

  push_images_to_test:
    if: needs.release.outputs.version != 'none'
    needs: [ release ]
    name: Push images to Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image: [
          'adminhost', 
          'backoffice-api', 
          'backoffice-commandhost', 
          'backoffice-eventhost', 
          'backoffice-extracthost', 
          'backoffice-ui', 
          'editor-projectionhost', 
          'extract-legacy',
          'import-legacy', 
          'producer-snapshot-projectionhost',
          'product-projectionhost', 
          'projector', 
          'synchost',
          'syndication-projectionhost', 
          'wfs-projectionhost', 
          'wms-projectionhost'
        ]
    steps:
      - name: Configure AWS credentials (Test)
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-access-key-id: ${{ secrets.VBR_AWS_ACCESS_KEY_ID_TST }}
          aws-secret-access-key: ${{ secrets.VBR_AWS_SECRET_ACCESS_KEY_TST }}
          aws-region: ${{ secrets.VBR_AWS_REGION_PRD }}

      - name: Login to Amazon ECR (Test)
        uses: aws-actions/amazon-ecr-login@v1.5.3

      - name: Download artifact
        uses: actions/download-artifact@v3
        continue-on-error: false
        with:
          name: ${{ matrix.image }}-${{ needs.release.outputs.version }}
          path: ~/

      - name: Load artifact
        shell: bash
        run: |
          echo rr-$IMAGE-image.tar
          docker image load -i ~/rr-$IMAGE-image.tar
        env:
          IMAGE: ${{ matrix.image }}

      - name: Push artifacts to ECR Test
        shell: bash
        run: |
          echo $IMAGE:$SEMVER
          docker push $BUILD_DOCKER_REGISTRY/road-registry/$IMAGE:$SEMVER
        env:
          BUILD_DOCKER_REGISTRY: ${{ secrets.VBR_BUILD_DOCKER_REGISTRY_TST }}
          IMAGE: ${{ matrix.image }}
          SEMVER: ${{ needs.release.outputs.version }}
          WORKSPACE: ${{ github.workspace }}

  deploy_to_test:
    if: github.repository_owner == 'Informatievlaanderen'
    needs: [ push_images_to_test, release, upload-lambda ]
    name: Deploy to test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        services: [
          'road-registry-adminhost',
          'road-registry-backoffice-api',
          'road-registry-backoffice-commandhost',
          'road-registry-backoffice-eventhost',
          'road-registry-backoffice-extracthost',
          'road-registry-backoffice-ui',
          'road-registry-editor-projectionhost',
          'road-registry-producer-snapshot-projectionhost',
          'road-registry-product-projectionhost',
          'road-registry-projector',
          'road-registry-synchost',
          'road-registry-syndication-projectionhost',
          'road-registry-wfs-projectionhost',
          'road-registry-wms-projectionhost'
        ]
    steps:
    - name: Deploy services
      env:
        BUILD_URL: ${{ secrets.VBR_AWS_BUILD_API }}/${{matrix.services}}
        STATUS_URL: ${{ secrets.VBR_AWS_BUILD_STATUS_API }}/${{matrix.services}}
      uses: informatievlaanderen/awscurl-polling-action/polling-action@main
      with:
          environment: test
          version: ${{ needs.release.outputs.version }}
          status-url: $STATUS_URL
          deploy-url: $BUILD_URL
          access-key: ${{ secrets.VBR_AWS_BUILD_USER_ACCESS_KEY_ID }}
          secret-key: ${{ secrets.VBR_AWS_BUILD_USER_SECRET_ACCESS_KEY }}
          region: eu-west-1
          interval: 2

    - name: Deploy services output
      shell: bash
      run: |
        echo build-uuid: ${{ steps.awscurl-polling-action.outputs.build-uuid }}
        echo Status: ${{ steps.awscurl-polling-action.outputs.status }}
        echo ${{ steps.awscurl-polling-action.outputs.final-message }}

  deploy_lambda_to_test:
    if: github.repository_owner == 'Informatievlaanderen'
    needs: [ deploy_to_test, release ]
    name: Deploy lambda to test
    runs-on: ubuntu-latest

    steps:
    - name: CD Lambda(s) Configure credentials
      uses: aws-actions/configure-aws-credentials@v1-node16
      with:
        aws-access-key-id: ${{ secrets.VBR_AWS_ACCESS_KEY_ID_TST }}
        aws-secret-access-key: ${{ secrets.VBR_AWS_SECRET_ACCESS_KEY_TST }}
        aws-region: ${{ secrets.VBR_AWS_REGION_PRD }}

    - name: Prepare Lambda(s)
      shell: bash
      run: |
        aws s3 cp s3://196194905350-vbr-test-lam-rr-sqsbackoffice/$VERSION/lambda.zip s3://196194905350-vbr-test-lam-rr-sqsbackoffice/lambda.zip --copy-props none
        aws s3 cp s3://196194905350-vbr-test-lam-rr-sqssnapshot/$VERSION/lambda.zip s3://196194905350-vbr-test-lam-rr-sqssnapshot/lambda.zip --copy-props none
      env:
        VERSION: ${{ needs.release.outputs.version }}

    - name: Promote Lambda(s)
      shell: bash
      run: |
        docker pull ghcr.io/okigan/awscurl:latest
        docker run --rm okigan/awscurl --access_key $ACCESS_KEY_ID --secret_key $SECRET_ACCESS_KEY_ID --region $REGION -X POST -d '{ "functionName": "rr-sqsbackofficefunction", "project": "basisregisters", "domain": "basisregisters" }' $PROMOTEURL/test
        docker run --rm okigan/awscurl --access_key $ACCESS_KEY_ID --secret_key $SECRET_ACCESS_KEY_ID --region $REGION -X POST -d '{ "functionName": "rr-sqssnapshotfunction", "project": "basisregisters", "domain": "basisregisters" }' $PROMOTEURL/test
      env:
        ACCESS_KEY_ID: ${{ secrets.VBR_AWS_ACCESS_KEY_ID_TST }}
        SECRET_ACCESS_KEY_ID: ${{ secrets.VBR_AWS_SECRET_ACCESS_KEY_TST }}
        REGION: ${{ secrets.VBR_AWS_REGION_PRD }}
        PROMOTEURL: ${{ secrets.VBR_AWS_PROMOTE_LAMBDA_BASEURL }}

  deploy_to_test_start_slack:
    if: github.repository_owner == 'Informatievlaanderen'
    needs: [ release, push_images_to_test ]
    name: Deploy to test started
    environment: test
    runs-on: ubuntu-latest

    steps:
    - name: Parse repository name
      run: echo REPOSITORY_NAME=$(echo ""$GITHUB_REPOSITORY"" | awk -F / '{print $2}' | sed -e ""s/:refs//"") >> $GITHUB_ENV
      shell: bash

    - name: Notify deployment started
      uses: slackapi/slack-github-action@v1.23.0
      with:
        channel-id: '#team-wegenregister-dev'
        slack-message: Deployment of road-registry ${{ needs.release.outputs.version }} to test has started. View run at ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      env:
        SLACK_BOT_TOKEN: ${{ secrets.VBR_SLACK_BOT_TOKEN }}
        SLACK_CHANNEL: ${{ secrets.VBR_NOTIFIER_CHANNEL_NAME }}
        REPOSITORY_NAME: ${{ env.REPOSITORY_NAME }}

  deploy_to_test_finish_slack:
    if: github.repository_owner == 'Informatievlaanderen'
    needs: [ release, deploy_lambda_to_test ]
    name: Deploy to test finished
    runs-on: ubuntu-latest

    steps:
    - name: Parse repository name
      run: echo REPOSITORY_NAME=$(echo ""$GITHUB_REPOSITORY"" | awk -F / '{print $2}' | sed -e ""s/:refs//"") >> $GITHUB_ENV
      shell: bash

    - name: Notify deployment finished
      uses: slackapi/slack-github-action@v1.23.0
      with:
        channel-id: '#team-wegenregister-dev'
        slack-message: ":white_check_mark: Deployment of road-registry ${{ needs.release.outputs.version }} to test has finished."
      env:
        SLACK_BOT_TOKEN: ${{ secrets.VBR_SLACK_BOT_TOKEN }}
        SLACK_CHANNEL: ${{ secrets.VBR_NOTIFIER_CHANNEL_NAME }}
        REPOSITORY_NAME: ${{ env.REPOSITORY_NAME }}
        
  push_images_to_staging:
    if: needs.release.outputs.version != 'none'
    needs: [ release ]
    name: Push images to Staging
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image: [
          'backoffice-api',
          'backoffice-ui',
          'backoffice-eventhost',
          'backoffice-extracthost',
          'backoffice-commandhost',
          'adminhost',
          'projector',
          'editor-projectionhost',
          'product-projectionhost',
          'producer-snapshot-projectionhost',
          'synchost',
          'syndication-projectionhost',
          'wms-projectionhost',
          'wfs-projectionhost'
        ]
    steps:
      - name: Configure AWS credentials (Staging)
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-access-key-id: ${{ secrets.VBR_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.VBR_AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.VBR_AWS_REGION_PRD }}

      - name: Login to Amazon ECR (Staging)
        uses: aws-actions/amazon-ecr-login@v1.5.3

      - name: Download artifact
        uses: actions/download-artifact@v3
        continue-on-error: false
        with:
          name: ${{ matrix.image }}-${{ needs.release.outputs.version }}
          path: ~/

      - name: Load artifact
        shell: bash
        run: |
          echo rr-$IMAGE-image.tar
          docker image load -i ~/rr-$IMAGE-image.tar
        env:
          IMAGE: ${{ matrix.image }}

      - name: Push artifacts to ECR Staging
        shell: bash
        run: |
          echo $IMAGE:$SEMVER
          docker tag $BUILD_DOCKER_REGISTRY_TST/road-registry/$IMAGE:$SEMVER $BUILD_DOCKER_REGISTRY/road-registry/$IMAGE:$SEMVER
          docker push $BUILD_DOCKER_REGISTRY/road-registry/$IMAGE:$SEMVER
        env:
          BUILD_DOCKER_REGISTRY_TST: ${{ secrets.VBR_BUILD_DOCKER_REGISTRY_TST }}
          BUILD_DOCKER_REGISTRY: ${{ secrets.VBR_BUILD_DOCKER_REGISTRY }}
          IMAGE: ${{ matrix.image }}
          SEMVER: ${{ needs.release.outputs.version }}
          WORKSPACE: ${{ github.workspace }}

  push_images_to_production:
    if: needs.release.outputs.version != 'none'
    needs: [ release ]
    name: Push images to Production
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image: [
          'adminhost', 
          'backoffice-api', 
          'backoffice-commandhost', 
          'backoffice-eventhost', 
          'backoffice-extracthost', 
          'backoffice-ui', 
          'editor-projectionhost', 
          'extract-legacy',
          'import-legacy', 
          'producer-snapshot-projectionhost',
          'product-projectionhost', 
          'projector', 
          'synchost',
          'syndication-projectionhost', 
          'wfs-projectionhost', 
          'wms-projectionhost'
        ]
    steps:
      - name: Configure AWS credentials (Production)
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-access-key-id: ${{ secrets.VBR_AWS_ACCESS_KEY_ID_PRD }}
          aws-secret-access-key: ${{ secrets.VBR_AWS_SECRET_ACCESS_KEY_PRD }}
          aws-region: ${{ secrets.VBR_AWS_REGION_PRD }}

      - name: Login to Amazon ECR (Production)
        uses: aws-actions/amazon-ecr-login@v1.5.3

      - name: Download artifact
        uses: actions/download-artifact@v3
        continue-on-error: false
        with:
          name: ${{ matrix.image }}-${{ needs.release.outputs.version }}
          path: ~/

      - name: Load artifact
        shell: bash
        run: |
          echo rr-$IMAGE-image.tar
          docker image load -i ~/rr-$IMAGE-image.tar
        env:
          IMAGE: ${{ matrix.image }}

      - name: Push artifacts to ECR Production
        shell: bash
        run: |
          echo $IMAGE:$SEMVER
          docker tag $BUILD_DOCKER_REGISTRY_TST/road-registry/$IMAGE:$SEMVER $BUILD_DOCKER_REGISTRY/road-registry/$IMAGE:$SEMVER
          docker push $BUILD_DOCKER_REGISTRY/road-registry/$IMAGE:$SEMVER
        env:
          BUILD_DOCKER_REGISTRY_TST: ${{ secrets.VBR_BUILD_DOCKER_REGISTRY_TST }}
          BUILD_DOCKER_REGISTRY: ${{ secrets.VBR_BUILD_DOCKER_REGISTRY_PRD }}
          IMAGE: ${{ matrix.image }}
          SEMVER: ${{ needs.release.outputs.version }}
          WORKSPACE: ${{ github.workspace }}

  push_images_to_newproduction:
    if: needs.release.outputs.version != 'none'
    needs: [ release ]
    name: Push images to New Production
    runs-on: ubuntu-latest
    strategy:
      matrix:
        image: [
          'adminhost', 
          'backoffice-api', 
          'backoffice-commandhost', 
          'backoffice-eventhost', 
          'backoffice-extracthost', 
          'backoffice-ui', 
          'editor-projectionhost', 
          'extract-legacy',
          'import-legacy', 
          'producer-snapshot-projectionhost',
          'product-projectionhost', 
          'projector', 
          'synchost',
          'syndication-projectionhost', 
          'wfs-projectionhost', 
          'wms-projectionhost'
        ]
    steps:
      - name: Configure AWS credentials (New Production)
        uses: aws-actions/configure-aws-credentials@v1-node16
        with:
          aws-access-key-id: ${{ secrets.VBR_AWS_ACCESS_KEY_ID_NEWPRD }}
          aws-secret-access-key: ${{ secrets.VBR_AWS_SECRET_ACCESS_KEY_NEWPRD }}
          aws-region: ${{ secrets.VBR_AWS_REGION_PRD }}

      - name: Login to Amazon ECR (New Production)
        uses: aws-actions/amazon-ecr-login@v1.5.3

      - name: Download artifact
        uses: actions/download-artifact@v3
        continue-on-error: false
        with:
          name: ${{ matrix.image }}-${{ needs.release.outputs.version }}
          path: ~/

      - name: Load artifact
        shell: bash
        run: |
          echo rr-$IMAGE-image.tar
          docker image load -i ~/rr-$IMAGE-image.tar
        env:
          IMAGE: ${{ matrix.image }}

      - name: Push artifacts to ECR New Production
        shell: bash
        run: |
          echo $IMAGE:$SEMVER
          docker tag $BUILD_DOCKER_REGISTRY_TST/road-registry/$IMAGE:$SEMVER $BUILD_DOCKER_REGISTRY/road-registry/$IMAGE:$SEMVER
          docker push $BUILD_DOCKER_REGISTRY/road-registry/$IMAGE:$SEMVER
        env:
          BUILD_DOCKER_REGISTRY_TST: ${{ secrets.VBR_BUILD_DOCKER_REGISTRY_TST }}
          BUILD_DOCKER_REGISTRY: ${{ secrets.VBR_BUILD_DOCKER_REGISTRY_NEWPRD }}
          IMAGE: ${{ matrix.image }}
          SEMVER: ${{ needs.release.outputs.version }}
          WORKSPACE: ${{ github.workspace }}

  build_notify_skipped:
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.set-release-version.outputs.version == 'none') }}
    name: Notify release skipped
    needs: [ set-release-version ]
    runs-on: ubuntu-latest
    steps:
      - name: Notify release skipped
        id: slack
        uses: slackapi/slack-github-action@v1.23.0
        with:
          channel-id: '#team-wegenregister-dev'
          slack-message: ":x: Release of road-registry has been skipped. View run at ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
        env:
          SLACK_BOT_TOKEN: ${{ secrets.VBR_SLACK_BOT_TOKEN }}
          SLACK_CHANNEL: ${{ secrets.VBR_NOTIFIER_CHANNEL_NAME }}
      - name: Fail release
        run: exit 1

  release_notify_skipped:
    if: ${{ (github.repository_owner == 'Informatievlaanderen') && (needs.release.outputs.version == 'none') }}
    name: Notify release skipped
    needs: [ release ]
    runs-on: ubuntu-latest
    steps:
      - name: Notify release skipped
        id: slack
        uses: slackapi/slack-github-action@v1.23.0
        with:
          channel-id: '#team-wegenregister-dev'
          slack-message: ":x: Release of road-registry has been skipped. View run at ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
        env:
          SLACK_BOT_TOKEN: ${{ secrets.VBR_SLACK_BOT_TOKEN }}
          SLACK_CHANNEL: ${{ secrets.VBR_NOTIFIER_CHANNEL_NAME }}
      - name: Fail release
        run: exit 1

  release_notify_failure:
    if: ${{ github.repository_owner == 'Informatievlaanderen' && failure() }}
    name: Notify release failed
    runs-on: ubuntu-latest
    needs: [ release ]
    steps:
      - name: Notify release failed
        id: slack
        uses: slackapi/slack-github-action@v1.23.0
        with:
          channel-id: '#team-wegenregister-dev'
          slack-message: ":x: Release of road-registry has failed. View run at ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
        env:
          SLACK_BOT_TOKEN: ${{ secrets.VBR_SLACK_BOT_TOKEN }}
          SLACK_CHANNEL: ${{ secrets.VBR_NOTIFIER_CHANNEL_NAME }}

  deployment_test_notify_failure:
    if: ${{ github.repository_owner == 'Informatievlaanderen' && failure() && needs.release.outputs.version && needs.release.outputs.version != 'none' }}
    name: Notify deployment failed
    runs-on: ubuntu-latest
    needs: [ deploy_to_test, release ]
    steps:
      - name: Notify deployment failed
        id: slack
        uses: slackapi/slack-github-action@v1.23.0
        with:
          channel-id: '#team-wegenregister-dev'
          slack-message: ":x: Deployment of road-registry ${{ needs.release.outputs.version }} to test has failed. View run at ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}"
        env:
          SLACK_BOT_TOKEN: ${{ secrets.VBR_SLACK_BOT_TOKEN }}
          SLACK_CHANNEL: ${{ secrets.VBR_NOTIFIER_CHANNEL_NAME }}
